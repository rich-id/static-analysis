#!/usr/bin/env bash

# Get Script directory
BIN_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )";

"$BIN_DIR"/phpinsights analyse ./src --no-interaction
"$BIN_DIR"/phpstan analyse
